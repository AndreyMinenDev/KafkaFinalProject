route:
  repeat_interval: 5m
  group_interval: 10s
  receiver: 'telegram'
receivers:
  - name: 'telegram'
    telegram_configs:
      - send_resolved: true
        api_url: https://api.telegram.org
        bot_token: "<BOT_TOKEN>"
        chat_id: "<CHAT_ID>"
